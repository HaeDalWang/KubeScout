apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubescout.fullname" . }}
  labels:
    {{- include "kubescout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubescout.selectorLabels" . | nindent 4 }}
